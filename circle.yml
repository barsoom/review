machine:
  services:
      - docker

dependencies:
  override:
    - script/ci/prepare.sh
  cache_directories:
    - ~/docker

test:
  override:
    - script/ci/tests.sh

#deployment:
#  production:
#    branch: master
#    heroku:
#      appname: exremit
