machine:
  services:
      - docker

dependencies:
  override:
    - script/ci/prepare.sh
  cache_directories:
    - ~/docker
    - ~/.mix
    - _build
    - deps
    - web/elm/elm-stuff

test:
  override:
    - script/ci/tests.sh

#deployment:
#  production:
#    branch: master
#    heroku:
#      appname: exremit
