machine:
  node:
    version: 4.2.2

dependencies:
  override:
    - script/ci/prepare.sh
  cache_directories:
    - ~/dependencies
    - ~/.mix

test:
  override:
    - script/ci/tests.sh

deployment:
  production:
    branch: master
    heroku:
      appname: exremit
